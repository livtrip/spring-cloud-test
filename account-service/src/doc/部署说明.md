### 用户模块接口说明

* 运行register
```text
docker run -p 8761:8761 --name myregister -d e55fc1f2cfde

http://111.231.189.44:8761
```
* account-service 运行脚本
```text
#!/bin/bash
cd /home/cloud
rm -rf spring-cloud-test
docker rmi $(docker images | grep "none" | awk '{print $3}')

git clone https://github.com/qccr-twl2123/spring-cloud-test.git

cd spring-cloud-test/account-service
mvn clean package -Dmaven.test.skip=true
mkdir target

mvn package -Dmaven.test.skip=true docker:build
#docker-compose up
docker rmi $(docker images | grep "none" | awk '{print $3}')
docker rm $(docker container ls -f "name=account-service" -q)
docker run -p 8762:8762 -d   --name account-service itmuch/account-service:0.0.1-SNAPSHOT

```

* nginx 
```text
server {
    server_name www.hao1024.cn
    listen 80;

    location / {
       add_header Access-Control-Allow-Origin *;
       add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
       add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';

       proxy_pass http://127.0.0.1:8080;
    }
}
```




